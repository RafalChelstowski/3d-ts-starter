export enum GuiLocalStorageKey {
  GUI_KEY = 'gui',
}

export enum GuiGroups {
  CAMERA = 'Camera',
  TEST_CUBE = 'Test Cube',
}

export enum PositionControl {
  POS_X = 'Position X',
  POS_Y = 'Position Y',
  POS_Z = 'Position Z',
}

export enum MaterialControlParam {
  WIREFRAME = 'wireframe',
}
